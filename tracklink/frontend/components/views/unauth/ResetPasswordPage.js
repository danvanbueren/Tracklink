import * as React from 'react';

export default function ResetPasswordPage({ slug }) {

    return (
        <main>
            <h2>Reset Password</h2>
            <p>slug: {slug}</p>
        </main>
    );
}